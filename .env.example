# 数据库连接（默认 SQLite，可换 PostgreSQL / MySQL）
DATABASE_URL="file:./phase.db"

# 可选：不设置时自动生成并存储到数据库
# 生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# CORS 来源（多个用逗号分隔，* 表示允许所有）
CORS_ORIGIN=*

# 服务端口
PORT=3000
