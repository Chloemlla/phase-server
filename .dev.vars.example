# 复制为 .dev.vars 并填入值
# 生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-here

# 必填（生产环境）：保护所有端点，防止未授权访问
# 生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INSTANCE_TOKEN=your-instance-token-here
